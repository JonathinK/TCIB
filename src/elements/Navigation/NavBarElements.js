import styled,{css} from "styled-components";

export const NavigationBar = styled.nav`
  width:100%;
  display:grid;
  grid-template-columns: minmax(2rem,1fr) minmax(auto,1920px) minmax(2rem,1fr);
  grid-template-rows:90px;
  background:rgba(200,50,100,.7);
  position:fixed;
  z-index:100;  
`
export const Logo = styled.div`

`






